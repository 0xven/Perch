## What changed and why

<!-- Summarize the change and the motivation. -->

## Checklist

- [ ] `npx tsc --noEmit` passes
- [ ] `npx eslint app components lib scripts --quiet` passes
- [ ] `npm run build` passes
- [ ] No secrets, API keys, or `.env.local` included in this diff
